# Posture Pro: 姿勢辨識演算法與運算架構詳解

本專案如何將攝影機的影像轉化為「駝背信心分數」？這背後結合了電腦視覺、人體工學幾何學以及訊號處理技術。以下是核心運算流程的完整解析。

---

## 1. 運算架構總覽 (Pipeline)

系統的運算流程分為五個階段：

1.  **影像採集**：透過 `CameraUI` 抓取 640x480 視訊幀。
2.  **特徵點偵測 (MediaPipe)**：定位全身 33 個關鍵點（Landmarks）。
3.  **幾何特徵提取**：計算軀幹夾角與頭部垂直位移。
4.  **特徵融合與評分 (Scorer)**：將幾何數值映射為 0.0~1.0 的信心分數。
5.  **狀態穩定化 (EMA)**：透過移動平均過濾雜訊，輸出最終判斷。

---

## 2. 核心特徵點選取

雖然 MediaPipe 提供了 33 個點，但為了提升運算效率與判斷穩定性，我們專注於以下關鍵索引：
*   **頭部**：鼻 (0)、眼 (1, 2)、耳 (7, 8)。
*   **軀幹**：肩 (11, 12)、臀 (23, 24)。

---

## 3. 演算法數學邏輯

### 第一階段：軀幹角度分析 ($S_A$)
目的：偵測「背部彎曲」或「坐姿前傾」。
*   **基準點**：
    *   $A = \text{耳中心}$ (兩耳中點)
    *   $B = \text{肩中心}$ (兩肩中點)
    *   $C = \text{臀中心}$ (兩臀中點)
*   **運算**：計算向量 $\vec{BA}$ 與 $\vec{BC}$ 的夾角 $\theta$。
*   **映射函數**：
    *   若 $\theta = 180^\circ$（完全挺直），則 $S_A = 0$。
    *   若 $\theta = 150^\circ$（駝背臨界），則 $S_A = 1$。
    *   公式：$S_A = \text{clamp}(\frac{180 - \theta}{30}, 0, 1)$。

### 第二階段：頭部傾斜分析 ($S_H$)
目的：偵測「烏龜頸」或「低頭看手機」。
*   **垂直位移**：$D_y = \text{眼中心}_Y - \text{耳中心}_Y$。
*   **比例正規化**：為了消除使用者與鏡頭距離的影響，我們引入 **臉部比例尺 ($W_{face}$)**。
    *   $W_{face} = |\text{左耳}_X - \text{右耳}_X|$。
*   **運算**：計算指標 $H_{index} = \frac{D_y}{W_{face}}$。
*   **映射函數**：$S_H = \text{clamp}(H_{index} \times 5, 0, 1)$（係數 5 為根據實驗得出的靈敏度常數）。

### 第三階段：MAX 融合 (Max Fusion)
系統採用「最嚴格判定」原則：
$$ConfidenceScore = \max(S_A, S_H)$$
這確保了無論是單純的「背彎」還是單純的「低頭」，系統都能維持高靈敏度。

---

## 4. 訊號處理：EMA 平滑化

AI 偵測到的原始座標會有微小跳動（Jitter）。如果直接使用原始分數，micro:bit 的畫面會不斷閃爍。我們導入了 **指數移動平均 (Exponential Moving Average)**：

$$EMA_{t} = \alpha \cdot Score_{t} + (1 - \alpha) \cdot EMA_{t-1}$$

*   **Alpha ($\alpha$)**：本專案設為 `0.3`。
*   **效果**：當分數快速變動時，EMA 會緩慢爬升。只有當「駝背動作」持續超過 3-5 幀時，系統才會正式更改狀態（isSlouching = true），有效過濾瞬間的誤判。

---

## 5. 動態門檻 (Dynamic Threshold T)

最終判斷邏輯非常簡單：
*   **IF $EMA \ge T$** $\rightarrow$ ❌ 姿勢不良 (通知硬體)。
*   **ELSE** $\rightarrow$ ✅ 良好。

透過網頁上的拉桿，您可以動態調整 $T$。
*   **校正功能**：當點擊「校正」時，系統會讀取您當前端正姿勢的 $Score$，並自動將 $T$ 設定在該數值上方 `+0.05` 處，達成個人化的精準監控。

---

## 結語

這套架構將複雜的 3D 人體動作拆解為純粹的幾何比例運算，不僅運算量極低（可在手機流暢執行），更具備了極強的抗干擾能力，是實現高效能 Web AI 應用的理想範例。
